{
  "name": "tetris",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run vite",
    "build": "bun run tsc && bun run vite build",
    "preview": "bun run vite preview"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}